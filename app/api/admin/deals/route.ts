import {NextResponse} from 'next/server'; import {cookies} from 'next/headers'; import {verifyAdmin} from '@/lib/security'; import {prisma} from '@/lib/prisma'
export async function POST(req:Request){if(!(await verifyAdmin((await cookies()).get('dn_admin')?.value))) return NextResponse.json({error:'Unauthorized'},{status:401}); const b=await req.json(); const value=Number(b.value||0), recurring=b.recurringValue?Number(b.recurringValue):null; const d=await prisma.deal.create({data:{leadId:b.leadId,value,recurringValue:recurring,status:b.status||'Open',notes:b.notes}}); return NextResponse.json(d)}
