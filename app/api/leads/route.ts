import {NextResponse} from 'next/server'; import {leadSchema} from '@/lib/validation'; import {prisma} from '@/lib/prisma'; import {referenceCode} from '@/lib/reference'; import {generateDiagnosis} from '@/lib/diagnosis'; import {track} from '@/lib/analytics'; import {sendEmail} from '@/lib/email'
export async function POST(req:Request){try{const input=leadSchema.parse(await req.json()); const code=referenceCode(); const lead=await prisma.lead.create({data:{referenceCode:code,name:input.name,email:input.email,phone:input.phone,companyName:input.companyName,companySize:input.companySize,industry:input.industry,problemDescription:input.problemDescription,currentWorkflow:input.currentWorkflow,desiredOutcome:input.desiredOutcome,painLevel:input.painLevel,frequency:input.frequency,budgetRange:input.budgetRange,contactMethod:input.contactMethod,status:'New',source:input.source,utmSource:input.utmSource,utmMedium:input.utmMedium,utmCampaign:input.utmCampaign,utmContent:input.utmContent,utmTerm:input.utmTerm,referrer:input.referrer,landingPage:input.landingPage,events:{create:{eventType:'Submitted',metadata:{dataSources:input.dataSources}}}}}); const diagnosis=await generateDiagnosis({...input}); await prisma.leadDiagnosis.create({data:{leadId:lead.id,problemCategory:diagnosis.problem_category,problemSummary:diagnosis.problem_summary,solutionTypes:diagnosis.likely_solution_types,estimatedComplexity:diagnosis.estimated_complexity,urgency:diagnosis.urgency,likelyServices:diagnosis.likely_services,missingInformation:diagnosis.missing_information,suggestedQuestions:diagnosis.suggested_questions,leadQuality:diagnosis.lead_quality,recommendedNextAction:diagnosis.recommended_next_action,rawAiOutput:diagnosis}}); await track('lead_created',{leadId:lead.id,source:input.source}); if(process.env.ADMIN_NOTIFICATION_EMAIL) await sendEmail({to:process.env.ADMIN_NOTIFICATION_EMAIL,subject:`New DataNizer lead ${code}`,html:`<p>New lead from ${input.companyName}.</p><p>${input.problemDescription}</p>`}); await sendEmail({to:input.email,subject:`DataNizer — request ${code} received`,html:`<p>Your request has been received.</p><p>Reference: <strong>${code}</strong></p>`}); return NextResponse.json({referenceCode:code})}catch(e:any){console.error(e);return NextResponse.json({error:e?.issues?.[0]?.message||'Could not submit request'},{status:400})}}
